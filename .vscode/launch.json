{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/apps/staff/node_modules/.vite/deps/package.json",
                "localRoot": "${workspaceFolder}/apps/staff/node_modules/.vite/deps"
            }
        }
    ]
}